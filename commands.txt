git init => initializing in present folder

git status => list all untracked files

git add -A => add all untracked files to staging area

git commit -m "msg" => to local repository

git remote add name path

git push -u name branch (master/main)